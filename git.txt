1) git checkout -b "Название ВЕТКИ" -- создает новую ветку
2) git branch -- просмотр всех веток
3) git add .
4) git push origin aiti -- залили на свою ветку

5) git restore --staged 'приложение\файл' -- возвращает до комиту
6) git revert HEAD --no-edit   -- удаление комита
7) git tag 'название тега' --- на нынешнюю ветку
8) git reser --hard 'символы ветки' -- жесткий сброс на ветку(удаляет оствльные)
9) git tag -d 'полностью название тега'  -- удаляет тег



